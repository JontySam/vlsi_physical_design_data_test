---
layout: layouts/base.njk
title: Guides
description: Browse concise VLSI physical design lab guides grouped by topic area.
permalink: /guides/
priority: 0.9
---
<section class="shell page-head">
  <p class="eyebrow">Guide library</p>
  <h1>All Guides</h1>
  <p class="lede">Short, checklist-driven notes for physical design study, implementation review, and interview preparation.</p>
</section>

<section class="shell section">
  {% for category in categories %}
  <section id="{{ category.slug }}" class="stack-block">
    <div class="section-head compact">
      <h2>{{ category.title }}</h2>
      <p>{{ category.description }}</p>
    </div>
    <div class="guide-grid">
      {% for topic in topics %}
        {% if topic.category == category.slug %}
      <article class="guide-card">
        <h3><a href="/guides/{{ topic.slug }}/">{{ topic.title }}</a></h3>
        <p>{{ topic.summary }}</p>
        <p class="meta">{{ topic.level }} | ~{{ topic.estimatedMinutes }} min | Updated {{ topic.updated }}</p>
      </article>
        {% endif %}
      {% endfor %}
    </div>
  </section>
  {% endfor %}
</section>
